package morsecode;

public class Morsecode
{
	
	public static void main(String[] args) throws Exception
	{
		
		
		
		
		Dot dot = new Dot();

		int count = 0;
		
		
	while (count <= 20)	
	{
		
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
		
		
			dot.getPause();
			dot.getDot();
			dot.getPause();
			
			dot.getDot();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			
			dot.getPause();
		
		
			dot.getDot();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			
			dot.getPause();
			
			dot.getLine();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
		
			
			dot.getWordPause();
		
			dot.getDot();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getLine();
		
			dot.getPause();
		
		
			dot.getLine();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
		
		
			dot.getPause();
			
			
			dot.getDot();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getDot();
			
			
			dot.getPause();
			
			
			dot.getDot();
			dot.getLetterPause();
			dot.getLine();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			
			
			dot.getPause();
			
			
			dot.getLine();
			dot.getLetterPause();
			dot.getDot();
			dot.getLetterPause();
			dot.getDot();
			dot.getWordPause();
			dot.getWordPause();
		
	count++;
	
	}
	}

}
